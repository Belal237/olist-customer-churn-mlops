## Tests

Pytest test suite.

Run the test suite:

```bash
pytest -v
```

Run with coverage report:

```bash
pytest -v --cov=src/features --cov-report=term-missing
```


- Unit tests for features
- API endpoint tests
- Minimum 70% coverage on critical code
